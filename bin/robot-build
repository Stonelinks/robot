#!/bin/bash
set -ex

if [ ! -d $PROJECT_ROOT/build ]; then
  mkdir $PROJECT_ROOT/build
fi

if [ -d $PROJECT_ROOT/build/$CAMERA_SKETCH ]; then
  rm -rf $PROJECT_ROOT/build/$CAMERA_SKETCH
fi

mkdir $PROJECT_ROOT/build/$CAMERA_SKETCH

arduino-cli compile \
 -o $PROJECT_ROOT/build/$CAMERA_SKETCH/$CAMERA_SKETCH.esp32.esp32.esp32 \
 --build-path $PROJECT_ROOT/build/$CAMERA_SKETCH \
 --fqbn esp32:esp32:esp32 $CAMERA_SKETCH.ino

 cp $PROJECT_ROOT/build/$CAMERA_SKETCH/$CAMERA_SKETCH.ino.bin $PROJECT_ROOT/build/$CAMERA_SKETCH/$CAMERA_SKETCH.esp32.esp32.esp32.bin
 cp $PROJECT_ROOT/build/$CAMERA_SKETCH/$CAMERA_SKETCH.ino.elf $PROJECT_ROOT/build/$CAMERA_SKETCH/$CAMERA_SKETCH.esp32.esp32.esp32.elf
 cp $PROJECT_ROOT/build/$CAMERA_SKETCH/$CAMERA_SKETCH.ino.partitions.bin $PROJECT_ROOT/build/$CAMERA_SKETCH/$CAMERA_SKETCH.esp32.esp32.esp32.partitions.bin